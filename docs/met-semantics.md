# MET Semantics

MET values are upstream source values unless explicitly marked as derived.

This repository should validate that importers preserve upstream MET values and do not merge activities with different MET levels.

HUMMBL-specific protocol mappings, coaching categories, or derived annotations must be stored separately from upstream values.
